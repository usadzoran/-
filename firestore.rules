rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all access to all collections
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
